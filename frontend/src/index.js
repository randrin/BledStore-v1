import React from 'react';
import ReactDOM from 'react-dom';
import './bledstore.scss';
import Bledstore from './bledstore';

ReactDOM.render(
  <React.StrictMode>
    <Bledstore />
  </React.StrictMode>,
  document.getElementById('bledstore')
);
